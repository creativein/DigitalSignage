<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="digital_signage_new.com"
    android:versionCode="1"
    android:versionName="1.0" >

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name" >
        <activity
            android:name=".LogIn"
            android:label="Take A Tour" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".slidemain"
            android:label="LogIn"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".FullVideo"
            android:label="FullVideo"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".SelectDownload"
            android:label="SelectDownload" />
        <activity
            android:name=".TwoSizeVideo"
            android:label="TwoSizeVideo"
            android:theme="@android:style/Theme.NoTitleBar" />
    </application>

    <uses-sdk android:minSdkVersion="3" />

    <uses-permission android:name="android.permission.WAKE_LOCK" >
    </uses-permission>
    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>

</manifest>